-- This script is responsible for handling the files and directories in the system.
-- It's job is provide a user interface to the file system.

-- The file system is a tree of directories and files. The root of the tree is the root directory.
-- Each directory has a name and a list of files and subdirectories. This is all handled from the c side
